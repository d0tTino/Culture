# Example Environment Variables for Culture Project
#
# The following variables are required for startup:
# LLM_API_BASE, MODEL_NAME and REDPANDA_BROKER.

# -- LLM Configuration --
# Base URL for your LLM backend (e.g., http://localhost:11434)
LLM_API_BASE=http://localhost:11434  # required
# Base URL for the vLLM server. When set, overrides ``LLM_API_BASE``
VLLM_API_BASE=http://localhost:8001
# Timeout in seconds for Ollama API requests
OLLAMA_REQUEST_TIMEOUT=10

# -- Redis Configuration --
# Hostname for the Redis server
REDIS_HOST=localhost
# Port for the Redis server
REDIS_PORT=6379
# Redis database number (optional, defaults typically 0)
REDIS_DB=0
# Redis password (if required, leave blank if none)
REDIS_PASSWORD=

# -- Other API Keys (Add as needed) --
# EXAMPLE_API_KEY=YOUR_API_KEY_HERE

# -- Vector Store Configuration --
# Base URL for your Weaviate instance (when using the Weaviate backend)
WEAVIATE_URL=http://localhost:8080
# Backend for the vector store ("chroma" or "weaviate")
VECTOR_STORE_BACKEND=chroma

# -- Model Configuration --
MODEL_NAME=mistral:latest  # required

# -- Policy Agent Service --
OPA_URL=http://opa

# -- Observability --
# Endpoint for OpenTelemetry log exporter
OTEL_EXPORTER_ENDPOINT=http://localhost:4318/v1/logs
# Set to 1 to activate OpenTelemetry export
ENABLE_OTEL=1

# -- Discord Bot Configuration --
# Token(s) used by the Discord bot (comma-separated for multiple)
DISCORD_BOT_TOKEN=
# ID of the Discord channel to use
DISCORD_CHANNEL_ID=

# -- Event Logging / Redpanda --
# Set to 1 to enable event logging and deterministic replay
ENABLE_REDPANDA=0
# Address of the Redpanda broker
REDPANDA_BROKER=localhost:9092  # required
# REDPANDA_TOPIC=culture.events  # change if your deployment uses a different topic name

# -- Snapshot Settings --
# Set to 1 to compress snapshots with zstandard
SNAPSHOT_COMPRESS=0

# -- vLLM Server (optional) --
# Model, port and swap space when using the vLLM backend
VLLM_MODEL=mistralai/Mistral-7B-Instruct-v0.2
VLLM_PORT=8001
VLLM_SWAP_SPACE=16
